import SettingsTabsClient from '@/components/admin/settings/SettingsTabsClient';

export const metadata = {
  title: 'Global Settings - Admin Panel'
};

export default function AdminSettingsPage() {
  return (
    <SettingsTabsClient />
  );
}
